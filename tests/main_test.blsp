;; Tests for the main module. `(:use main)` brings `main` into scope;
;; `(:use test)` the describe/test/is macros. `nest test` loads src/ first.
(defmodule main-test (:use main) (:use test))

(describe "main"
  (test "main is callable" (is (fn? main))))
